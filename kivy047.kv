MDBoxLayout:
    orientation:  'vertical'

    MDScreen:

        MDLabel:
            id: my_label
            text: "Some stuff"
            halign: "center"

        MDFloatingActionButtonSpeedDial:
            data: app.data
            root_button_animation: True
            # color stuff
            # label_text_color: 1, 0, 0, 1 #text color
            # bg_color_stack_button: 1,0,0,1  #bg icon
            # bg_color_root_button: 1,0,0,1   #speed dial bg
            # color_icon_root_button: 1,0,0,1     #sppeed dial text color
            # color_icon_stack_button: 1,0,0,1       #icon text color
            # bg_hint_color: 1,0,0,1  #icon bg color dynamic

            # pushing button
            callback: app.callback

            # open or close
            on_open: app.open()
            on_close: app.close()
